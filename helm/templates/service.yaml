apiVersion: v1
kind: Service
metadata:
  name: {{ include "elderlycare-web.fullname" . }}
  labels:
    {{- include "elderlycare-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "elderlycare-web.selectorLabels" . | nindent 4 }}
