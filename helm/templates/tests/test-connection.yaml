apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "elderlycare-web.fullname" . }}-test-connection"
  labels:
    {{- include "elderlycare-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "elderlycare-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
